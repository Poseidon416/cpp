#include <iostream>
#include <string>
#include "LinkedList.h"

using namespace std;

int main(){
  LinkedList* hashTable = new LinkedList[100];
  bool reHash = false;
  hashTable[0].add(reHash);  
  return 0;
}
